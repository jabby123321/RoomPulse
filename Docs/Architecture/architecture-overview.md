# Architecture overview

RoomPulse aims to provide sensor aggregation and display for several embeedded devices viewable on a web interface.

The system has three main components: the web frontend (for displaying sensor data to the end user), the web API (allowing sensors to store data and the frontend to retreive data) and the sensor firmware.

![Component diagram showing system architecture](.images/component-diagram.png)

